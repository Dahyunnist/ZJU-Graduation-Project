# 第二、第三个正式分片运行与验收记录

## 总体状态

2026-08-11 按冻结配置依次运行 `s2026__P1__c2st_lr` 与 `s2026__P1__c2st_xgb`。第二分片完成并通过完整性验收后才启动第三分片；两者没有并行运行，也没有继续启动第四个深度分片。当前正式进度为 3/110，剩余 107 个分片，下一项为 `s2026__P1__flat_transformer`。

## 第二分片：C2ST-LR

- 状态：`complete`；`formal_inclusion=true`
- 运行时间：449.14 秒（约 7.5 分钟）
- 证据：35,100/35,100 行
- 重复键、非有限估计、量化器失败：均为 0
- 每个预注册组合：100 个 bag
- 最大误差分解残差：1.11e-16
- 平均污染率 MAE：0.0810
- 目标测试 AUROC：0.7100
- source/oracle 目标 FPR：0.0463；目标真实锚点 FPR：0.0637

该分片的估值记录为 0，符合分片所有权设计：同一 seed/protocol 的 26,000 行估值已经由首个 Char 3-gram 分片唯一计算，后续检测器不得重复估值。

## 第三分片：C2ST-XGBoost

- 状态：`complete`；`formal_inclusion=true`
- 运行时间：445.59 秒（约 7.4 分钟）
- 证据：35,100/35,100 行
- 重复键、非有限估计、量化器失败：均为 0
- 每个预注册组合：100 个 bag
- 最大误差分解残差：2.78e-17
- 平均污染率 MAE：0.0256
- 目标测试 AUROC：0.9397
- source/oracle 目标 FPR：0.0473；目标真实锚点 FPR：0.0643

C2ST-XGBoost 的预检过程建立了约 424 MiB 的 CUDA 上下文，但 GPU 利用率持续为 0%，模型本身使用 `tree_method=hist` 与 CPU 线程运行。占用进程始终是当前正式分片，没有其他 GPU 计算进程；分片结束后显存立即恢复至 17 MiB。

## 验收结论

两个分片的完整性、信息边界和数值稳定性全部通过。`target_real_anchor` 始终不使用目标来源标签，`oracle_target` 标签标记正确。三种策略不改变 AUROC 排序，误差分解恒等式达到浮点精度。第二分片通过后自动开始第三分片的执行逻辑符合要求，第三分片完成后已经停止。

下一项是首个深度检测分片 `s2026__P1__flat_transformer`。该分片将实际使用共享 GPU，必须继续采用单分片运行，并重点验收训练稳定性、峰值显存、GPU 利用率、运行时间和其他用户进程门禁。
